// Canonical definition lives in @secondseat/db — re-exported here for backward compatibility.
export {
  RunContextModel,
  type IRunContext,
  type PlayerGoal,
  type ConfidenceLevel,
} from "@secondseat/db";
